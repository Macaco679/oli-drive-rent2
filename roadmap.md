# Roadmap

## Tarefas concluídas
- [x] Remover filtro `owner_id` de `getVehicleById` em `src/lib/vehicleService.ts`
- [x] Corrigir erros de typecheck que impedem o build do preview
  - [x] `src/pages/Search.tsx` — interface `VehicleWithCover` incompatível com `OliVehicle`
  - [x] `src/pages/SecuritySettings.tsx` — tipo `VerifiedFactor` incompatível com retorno do Supabase MFA

